name: My First Pipeline

# When should this run?
on: [push]

# What should it do?
jobs:
  build:
    runs-on: ubuntu-latest  # Rent a Linux server (free!)
    steps:
      - name: Say Hello
        run: echo "Hello DevOps World! My pipeline is active."
      
      - name: Check System Info
        run: uname -a
